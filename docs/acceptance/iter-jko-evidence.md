# 迭代批 J–O 验收证据（2026-08-06 晚 · 第三轮反馈 9 条）

> 自动化基线：77 测试全绿、engine/web 双包 tsc 通过（每批 commit 前验证）。
> 人工验收为浏览器实操（:5177）+ curl 实测（:4177）。

## 批J 血缘分段流动动画（问题1）

- Action 提交播右半环（本体→Action→写回→审计，金色流动箭头）；重物化播左半环（数据集→Funnel→本体，蓝色流动箭头）——此前重物化无动画，本批补齐。
- 歧义分析结论：全条流动会暗示"重物化也发生了"，分段流动才与实际发生的因果一致；节点按语义(蓝)/动能(金)本色发光。

## 批K 决策活动页（问题2）

- store/server/api 加 offset 参数（默认 0，MCP/CLI 零破坏）。
- 右栏精简：审计 8 条 + 通知 6 条，各带"全部 →"。
- 活动页实测：第 1 页 20 条 →"下一页"→ 第 2 页 2 条（当时共 22 条审计吻合）且下一页禁用（尾页判断）✅。

## 批L 概念页 + 数据流向图（问题3）

- 概念地图从浮层改独立页（◈ 按钮入导航历史，可返回/前进）。
- 新增"数据流向"节：SQLite 单文件 ontology.db 说明（obj_*×6 / object_edits / audit_log / notifications；"CSV+编辑才是真相源"）、五层分层图（问财→CSV→存储→引擎六模块→四消费端）、写路径治理不变量卡。截图验证完整渲染。

## 批M 引用对象预览卡（问题5）

- 任何 objectRef 参数选中后显示所指对象当前内容（renderCell 复用：翻名/徽章/条件中文化）。
- 实测：组合估值选 P2 → 预览卡显示"组合名称 半导体设备材料 / 初始资金 1,000,000 / 现金 800,000"。改研判选笔记即见原文与关联股票。

## 批N Function 结果友好化（问题7）

- 实测组合估值 P2：裸 JSON → KPI 卡行（totalAssets 914,055 / totalPnl **-85,945 绿色跌色**）+ positions(1) 子表格（华海清科行含盈亏色）。纯形状驱动，不特判 Function。

## 批O Web AI 对话入口 + README（问题8/9）

- 双驱动桥（chat-bridge.ts，部署组装层非 src/）：无 ANTHROPIC_API_KEY→spawn 本机 claude CLI（订阅零 key）；有→SDK 进程内 runloop。密钥只存环境变量/登录态，仓库零密钥。
- **实测 2 次全通**（driver=claude-cli）：
  1. "P1 总资产？"→ ToolSearch → fn_portfolioValuation → 正确回答 934,043 元；
  2. "筛 pe<40 报数量与前三"→ fn_screenStocks → "12 只：金山办公/佰维存储/传音控股"（带真实 PE/市值表格）。
  - 首测 final 尾部混入本机全局 Stop hook 文案（工作树脏触发子会话落盘提醒）；commit 干净后复测消失——已确认为环境性而非桥缺陷。
- ChatDock UI：浮动入口/4 条示例任务/工具调用时间线（⚙ 折叠）/驱动标签/AI 写入触发全局刷新+血缘动画。
- 根 README：定位"复刻 Ontology 的工程实践"，含本体论思考 8 条、mermaid 架构图、三个签名设计决策、双凭据跑法、演示动线；`grep 面试|interview|简历|resume` 全仓扫描仅命中会话恢复语义的 "resume"，无求职表述。

## 本会话 MCP 链条演示（问题6 实证）

1. `get_object ResearchNote RN-1` → 当前内容（含新列 title:null——演进迁移后旧进程也能读）；
2. `action_writeResearchNote` → RN-5 创建成功（旧工具集进程写 5 列、title 自动 NULL——**向后兼容实证**）；
3. `list_audit` → 审计 #23：谁/何时/什么参数/什么编辑完整在案。
- 另一实证：本会话 MCP 工具清单是**连接时快照**（14 工具，无批C 后新增动词）——"工具清单静态注入、数据动态查询"的直接证据。

## 诚实边界

- ChatDock 的浏览器内实际发送未走全程（后端链路已 curl 实测 2 次；UI 侧渲染/示例/输入均已验证）——用户点一条示例即可完成端到端首验，耗订阅额度。
- API 驱动（ANTHROPIC_API_KEY 路径）未实跑（本机无 key，逻辑与 api-agent.ts 同构且 tsc 通过）。
- 血缘流动动画为瞬态（截图难留证）：CSS keyframes + 分段类名逻辑经代码与类名核验，用户提交任一 Action 即可肉眼验证。
