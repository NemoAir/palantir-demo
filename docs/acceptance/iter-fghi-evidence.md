# 迭代批 F/G/H/I 验收证据（2026-08-06）

> 对应用户第二轮反馈 15 条（13 条编号问题 + 追加 2 条）。
> 自动化基线：**77 测试全绿**（16→17 文件；新增 OMS 校验 3 例、动词新契约 1 例、演进迁移 2 例）；engine/web 双包 tsc 通过。
> 以下人工验收均为浏览器实操（http://localhost:5177，API :4177 重启后带新元数据）。

## 批F 概念统一 + 页面自解释

| 验收点 | 结果 | 证据 |
|---|---|---|
| docs 元数据（8 Action + 3 Function） | ✅ | 模拟调仓/预警扫描等面板顶部出现蓝色说明块（如"巡逻员：…只有眼睛没有手"）；`/api/schema` 回读 `trade.docs` 非空；AI 工具 description 拼入 docs |
| 导航分组抽屉 + 分色 | ✅ | 截图：「语义元素 · 名词」蓝标题 /「动能元素 · 动词」金标题，内分 ACTION·写入 / FUNCTION·只读计算 / 系统 Action 折叠 |
| 概念地图 | ✅ | 顶栏 ◈ 按钮 → 右侧抽屉，三节 8 卡（本体/语义/动能/接入/物化/重放/审计/闭环），Esc 可关 |
| 通知可点击视觉 | ✅ | JS 核验：7 条 linked 通知，金左边框 rgb(138,109,47)、cursor:pointer、"→ 点击查看"后缀 |

## 批G 引用可读化（元模型四扩展，TDD 3 例先红后绿）

| 验收点 | 结果 | 证据 |
|---|---|---|
| titleProperty 外键翻名 | ✅ | 预警/研判列表股票代码列显示"688981 中芯国际"等；审计参数"股票 688120 华海清科"；选择器已选"688981 中芯国际" |
| enumOptions 彩色徽章 | ✅ | 预警状态：已触发红/待命蓝/已处理灰；研判结论：看多红/中性灰 |
| enumOptions 过滤值下拉 | ✅ | 过滤属性切"状态"→ 值栏变下拉（待命/已触发/已处理）；选 armed 过滤 → `status~armed` chip，5→4 条（AL-1 已触发被滤掉） |
| format=filterExpr 条件中文化 | ✅ | 条件列"latestPrice<200 最新价 低于 200"、"pe<30 市盈率PE(TTM) 低于 30"、"latestPrice<=300 最新价 不高于 300" |
| ParamDef.hint 最新价提示 | ✅ | 调仓选中芯国际 → 成交价下方"当前最新价：121.10（点击填入）"，点击后 price=121.1 |
| OMS 新校验 | ✅ | titleProperty 指向不存在属性/format 引用未知类型/hint 三项非法均被拒（测试红→绿） |

## 批H 本体演进 + 系统动词收紧

| 验收点 | 结果 | 证据 |
|---|---|---|
| ResearchNote.title 演进 | ✅ | 列表自动出现"标题"列（旧数据 —）；写研判/改研判表单自动多标题字段 |
| updateResearchNote 新契约 | ✅ | 测试：只改标题其余保留、全缺省被 atLeastOneField 拒 |
| **演进迁移机制**（补坑） | ✅ | 发现真实坑：旧库 obj_ResearchNote 无 title 列且纯编辑型无重物化兜底 → init() 自动 ALTER 补 nullable 列（真库 PRAGMA 回读第 5 列 title、旧行 NULL、API 正常）；非空新列指名报错。TDD 2 例 |
| 系统动词收紧 | ✅ | 标记预警触发页：金色说明块 + 表单收进"演示用"折叠 |
| 扫描结果一键落账 | ✅ | checkAlerts 命中 AL-5（华海清科 228.11≤300）→ 行尾"⚙ 标记预警触发"→ 点击 → 自动重扫变（空结果）→ 审计 #22"标记预警触发 预警 AL-5" + 通知"预警触发：688120 满足 latestPrice<=300" |

## 批I 活系统驱动

| 验收点 | 结果 | 证据 |
|---|---|---|
| **claude -p 实跑**（零 API key） | ✅ | 命令：`claude -p "查 P1 估值…" --allowedTools "mcp__astock-ontology__fn_portfolioValuation"`；返回真实总结："P1「学习演示组合」总资产 934,043 元（现金 548,687 + 持仓市值 385,356），浮动亏损 66,950，主要拖累中微公司 -72,905"——数字高熵可交叉验证，订阅计费、零 runloop 代码 |
| daily-patrol.sh | ✅ 已交付 | 重物化 + claude -p 巡检（工具白名单=治理边界），chmod +x |
| launchd 模板 + README | ✅ 已交付 | 每交易日 15:35，用户自行 `launchctl load` 激活（未替用户改系统配置） |

## 诚实边界

- daily-patrol.sh **整体未实跑**（其中 claude -p 巡逻含写入动词，会真实改库+耗订阅额度）；其只读同构链路已实测通过（上表）。首跑建议用户亲自执行观察。
- launchd 定时**未加载**——改系统持久配置需用户自行执行（README 有命令）。
- 行情自动拉取（问财 OpenAPI 独立取数脚本）未实现——现行边界：行情由对话内 Claude 拉取（CLAUDE.md 数据接入边界），README 已标注"要做时说一声"。
