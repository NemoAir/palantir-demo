{
  "name": "palantir-demo",
  "private": true,
  "scripts": {
    "test": "pnpm --filter engine test",
    "cli": "pnpm --filter engine cli"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}
